import 'package:flutter/material.dart';

// Primary colors
const Color appPrimaryColor = Color.fromRGBO(0, 122, 255 , 1); // blue
const Color appSecondaryColor = Color.fromRGBO(128, 128, 128, 0.55); // light grey

// Navbar Top & Bottom
const Color appNavBarColor = Color.fromRGBO(255, 255, 255, 1); // smooth withe

// Icons
const Color appSelectedIconColor = Color.fromRGBO(0, 122, 255 , 1); // blue
const Color appUnselectedIconColor = Color.fromRGBO(128, 128, 128, 0.55); // light grey

// Labels
const Color normalLabel = Color.fromRGBO(0, 0, 0, 1);
const Color highlightLabel = Color.fromRGBO(0, 122, 255, 1);

// Backgrounds
const Color appBackgroundColor = Color.fromRGBO(242, 242, 247, 1); // background grey


// Spacing
const double spacingSmall = 10.0;
const double spacingNormal = 20.0;
const double spacingBig = 32.0;
const double spacingXL = 50.0;


// Fonts
const double primaryHeading = 28.0;
const double secondaryHeading = 18.0;


// Border Radius
const double borderRadiusSmall = 10.0;


// Padding
const double paddingSmall = 10.0;
const double paddingNormal = 15.0;
const double paddingBig = 20.0;
